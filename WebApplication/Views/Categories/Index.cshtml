@model IEnumerable<WebApp.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.category_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.category_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.category_type">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.category_type">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.category_type">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
